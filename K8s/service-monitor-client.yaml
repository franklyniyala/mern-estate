apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mern-client-sm
  labels:
    release: prometheus
spec:
  selector:
      matchLabels:
        app: mern-client
  namespaceSelector:
    any: true
  endpoints:
  - port: http
    interval: 15s
    path: /metrics