apiVersion: v1
kind: Service
metadata:
  name: mern-client
spec:
  selector:
    app: node-client
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5173
  type: LoadBalancer